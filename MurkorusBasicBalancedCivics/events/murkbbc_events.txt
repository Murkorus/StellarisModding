namespace = murkbbc_event

# Sets global flag so the game knows this mod is installed
country_event = {
	id = murkbbc_event.1
	hide_window = yes
	fire_only_once = yes
	trigger = {
		NOT = {
			has_global_flag = murkbbc_installed
		}
	}
	immediate = {
		set_global_flag = murkbbc_installed
	}
}
